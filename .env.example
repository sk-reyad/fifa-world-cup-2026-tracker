# Optional only. The default project uses the free worldcup26.ir API and does not need a token.
# If worldcup26.ir later requires a JWT token, put it in Vercel as WORLDCUP26_API_TOKEN.
WORLDCUP26_API_BASE=https://worldcup26.ir
# WORLDCUP26_API_TOKEN=optional_jwt_token_if_required_later
